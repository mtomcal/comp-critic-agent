# OpenAI API Configuration
OPENAI_API_KEY=your-api-key-here

# Optional: Specify which model to use
# OPENAI_MODEL=gpt-4.1  # Default model (recommended for vision + long context)
# Available options: gpt-4.1, gpt-4.1-mini, gpt-4o, gpt-4o-mini

# Optional: Customize paths
# TRANSCRIPTS_DIR=./transcripts
# CHROMA_DB_PATH=./chroma_db

# Optional: Vision API Configuration
# VISION_DETAIL=low  # "low" (85 tokens, faster) or "high" (more tokens, better quality)
# VISION_MAX_SIZE=2048  # Maximum image dimension in pixels

# Optional: RAG Configuration
# RAG_TOP_K=3  # Number of transcript chunks to retrieve
# CHUNK_SIZE=1000  # Characters per chunk during ingestion
# CHUNK_OVERLAP=200  # Character overlap between chunks
